# 云端名称同步清单

本清单用于将项目中已改为 **前呈似景智链** 的代码和文案，同步到当前云数据库已有业务数据中，避免出现“代码是新名字，云端还是旧名字”的情况。

## 使用方式

- 建议按顺序处理，优先改首页直接可见内容，再改后台和历史数据。
- 处理完成后，可在小程序前台和后台页面逐项验证。

## 一、首页与店铺展示

- [ ] `home_contents.storeName` 已改为 `前呈似景智链`
- [ ] `home_contents.notice` 中旧名字已替换为 `前呈似景智链`
- [ ] `home_contents.bannerTitle` 已确认
- [ ] `home_contents.servicePhone` 已确认
- [ ] 店铺设置页提交审核并已通过

建议验证页面：

- 首页顶部品牌名
- 首页滚动公告
- 店铺设置页

## 二、管理员与平台自有账号资料

重点集合：`users`

建议检查字段：

- `company`
- `addressDetail`
- `remark`

重点检查对象：

- 超级管理员账号
- 管理员账号
- 平台自有客户/演示客户账号

建议处理项：

- [ ] 管理员 `company` 已改为 `前呈似景智链`
- [ ] 平台仓配地址中的旧名字已改为 `前呈似景智链仓配中心`
- [ ] 备注中无旧名字残留

## 三、订单与地址快照

重点集合：

- `orders`
- `addresses`

建议检查字段：

- `customerName`
- `addressSnapshot.contactName`
- `addressSnapshot.detail`
- `remark`
- `addresses.contactName`
- `addresses.detail`

说明：

- 如果这些字段是客户门店自己的真实名称，一般不需要改。
- 只有当这些字段里写的是平台/仓配名称，且仍是旧名字时，才需要同步。

建议处理项：

- [ ] 历史订单里无错误平台名称残留
- [ ] 地址快照里无旧仓配名称残留

## 四、对账与导出

代码里已改为：

- `前呈似景智链配送中心`

但仍建议检查：

- 导出的对账单页面
- 预结单抬头
- 导出备注说明

建议处理项：

- [ ] 对账导出标题显示为 `前呈似景智链配送中心`
- [ ] 对账导出内容中无旧品牌名残留

## 五、协议与说明

建议检查页面：

- `我的 -> 协议及说明 -> 用户协议`
- `我的 -> 协议及说明 -> 隐私协议`

建议处理项：

- [ ] 用户协议正文已使用 `前呈似景智链`
- [ ] 隐私协议正文已使用 `前呈似景智链`
- [ ] 审核说明文档已使用 `前呈似景智链`

## 六、seed / 演示数据

如果你还在使用演示数据或准备重新初始化：

- [ ] 已重新上传部署 `cloudfunctions/bootstrap`
- [ ] 确认 seed 中默认店铺名为 `前呈似景智链`
- [ ] 如需重新灌演示数据，已重新初始化演示数据

说明：

- 代码里的 seed 已改成新名字
- 但数据库里**已存在**的旧数据不会自动改

## 七、同步完成后建议验证

建议至少检查以下页面：

- [ ] 首页
- [ ] 登录页
- [ ] 申请页
- [ ] 店铺设置页
- [ ] 商品详情页
- [ ] 对账中心导出预览
- [ ] 协议及说明页

## 当前统一名称

- 小程序名：`前呈似景智链`
- 店铺名：`前呈似景智链`
- 对账抬头：`前呈似景智链配送中心`
